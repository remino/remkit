# Palette copied from the theme contract in ~/.config/tmux/tmux.conf.
# Replace only the existing color_* assignments with these values. The
# status-format, status-left, and status-right content is intentionally not
# included or changed.

color_orange="colour166" # 208, 166
color_purple="colour134" # 135, 134
color_green="colour076" # 070
color_blue="colour39"
color_yellow="colour220"
color_red="colour160"
color_black="colour232"
color_white="white" # 015

color_dark="$color_black"
color_light="$color_white"
color_session_text="$color_blue"
color_status_text="colour245"
color_main="$color_blue"
color_secondary="$color_purple"
color_level_ok="$color_green"
color_level_warn="$color_yellow"
color_level_stress="$color_red"
color_window_off_indicator="colour088"
color_window_off_status_bg="colour238"
color_window_off_status_current_bg="colour254"

# Optional non-content styling. These are the same settings already present
# in the config, expressed with the Cyberwave variables above.
set -g mode-style "fg=$color_dark,bg=$color_main"
set -g message-style "fg=$color_main,bg=$color_dark"
set -g status-style "fg=$color_status_text,bg=default"
setw -g window-status-current-style "fg=$color_light,bold,bg=$color_main"
setw -g pane-active-border-style "fg=$color_main"
