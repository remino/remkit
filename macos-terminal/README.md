# macos-terminal

Import a profile by opening its `.terminal` file in Terminal, then select it in
Terminal > Settings > Profiles.

The profiles use the compatible Terminal settings from `iterm/profiles/Rem.json`
and apply the corresponding palette from `iterm/themes/`.
